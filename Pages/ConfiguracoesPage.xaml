<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UAUIngleza_plc.Pages.ConfiguracoesPage"
             Title="Configuração PLC"
             BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1a1a1a}">

    <ScrollView Padding="20,20,20,100">
        <VerticalStackLayout Spacing="20">

            <Label Text="⚙️ Configuração do Sistema" 
                   FontSize="28" 
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#FFFFFF}"
                   Margin="0,10,0,20" />

            <VerticalStackLayout Spacing="8">
                <Label Text="📡 Endereço IP:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />
                <Entry x:Name="IpEntry" 
                       Text="{Binding IpAddress}" 
                       Placeholder="192.168.1.100"
                       Keyboard="Telephone"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="🔧 Rack:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />

                <Entry Text="{Binding Rack}" 
                       Placeholder="0"
                       Keyboard="Numeric"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="📍 Slot:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />

                <Entry Text="{Binding Slot}" 
                       Placeholder="0"
                       Keyboard="Numeric"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="🖥️ Endereço IP da câmera"
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#666}" />

                <Entry Text="{Binding CameraIp}"
                       Placeholder="192.168.1.200:60000"
                       Keyboard="Telephone"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <Border Padding="15"
                    Margin="0,10,0,0"
                    BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#222}">
                    <Label Text="{Binding StatusMessage}" 
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#555, Dark=#BBB}"
                       LineBreakMode="WordWrap" />
            </Border>

            <Border Padding="15"
                    x:Name="StatusBorder"
                    Margin="0,10,0,0"
                    BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#222}">
                    <Label Text="{Binding StatusConnection}"
                       FontSize="14"
                       x:Name="StatusLabel"
                       TextColor="{AppThemeBinding Light=#555, Dark=#BBB}"
                       LineBreakMode="WordWrap" />
            </Border>

            <VerticalStackLayout Spacing="12" Margin="0,20,0,0">
                <Button Text="💾 Salvar Configuração" 
                        Clicked="OnSaveConfigClicked"
                        BackgroundColor="#2196F3"
                        TextColor="White"
                        FontAttributes="Bold"
                        Padding="15,12"
                        CornerRadius="8"
                        FontSize="16" />

                <Button Text="🔗 Conectar ao PLC" 
                        Clicked="OnConnectClicked"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        FontAttributes="Bold"
                        Padding="15,12"
                        CornerRadius="8"
                        FontSize="16" />
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>