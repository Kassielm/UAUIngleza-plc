<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UAUIngleza_plc.Pages.ConfiguracoesPage"
             Title="Configuração PLC"
             BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#1a1a1a}">

    <ScrollView Padding="20,20,20,100">
        <VerticalStackLayout Spacing="20">

            <Label Text="⚙️ Configuração do Sistema" 
                   FontSize="28" 
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#1a1a1a, Dark=#FFFFFF}"
                   Margin="0,10,0,20" />

            <VerticalStackLayout Spacing="8">
                <Label Text="📡 Endereço IP:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />
                <Entry x:Name="IpEntry"
                       Placeholder="192.168.2.1"
                       Keyboard="Url"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="🔧 Rack:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />

                <Entry x:Name="RackEntry"
                       Placeholder="0"
                       Keyboard="Numeric"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="📍 Slot:" 
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />

                <Entry x:Name="SlotEntry"
                       Placeholder="1"
                       Keyboard="Numeric"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="8">
                <Label Text="📹 Endereço IP da Câmera"
                       FontAttributes="Bold"
                       FontSize="14"
                       TextColor="{AppThemeBinding Light=#333, Dark=#DDD}" />

                <Entry x:Name="CameraEntry"
                       Placeholder="http://192.168.0.101"
                       Keyboard="Url"
                       PlaceholderColor="{AppThemeBinding Light=#999, Dark=#666}"
                       TextColor="{AppThemeBinding Light=#000, Dark=#FFF}"
                       BackgroundColor="{AppThemeBinding Light=#FFF, Dark=#2a2a2a}"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="12" Margin="0,20,0,0">
                <Button Text="💾 Salvar Configuração" 
                        Clicked="OnSaveConfigClicked"
                        BackgroundColor="#2196F3"
                        TextColor="White"
                        FontAttributes="Bold"
                        Padding="15,12"
                        CornerRadius="8"
                        FontSize="16" />
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>